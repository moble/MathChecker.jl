var documenterSearchIndex = {"docs":
[{"category":"section","location":"10-checks/#The-checks","page":"The checks","text":"A Checked has eight type parameters:\n\nChecked{T, Precision, NaN, Inf, Cancellation, Absorption, Subnormal, Rounding}\n\nT is the wrapped floating-point type.  Each of the remaining seven is a flag: false turns the check off and true turns it on. Three of the flags accept a configuration value in place of true, described below.  The flags can be given positionally, or by keyword to checked, Checked{T}(x; ...), and Checked(x; ...):\n\nChecked{Float64, true, true, true, 1e-6, false, false, false}(x)\nchecked(x; cancellation=1e-6)          # the same, with the other flags at their defaults\n\nThe defaults (DEFAULT_FLAGS) turn on Precision, NaN, and Inf — the three checks that can never produce a false positive — and leave the rest off.\n\nEvery check is evaluated on the unwrapped values, after the operation has been computed by the wrapped type's own method.  A Checked{Float64} therefore always computes exactly what a Float64 would; the checks only observe.  When a check fails it calls the current handler, which by default throws.","title":"The checks"},{"category":"section","location":"10-checks/#Precision","page":"The checks","text":"Signals when the value is implicitly mixed with a floating-point value of a different type.  A Checked{Float32} cannot be added to a Float64; a Checked{Double64} cannot be multiplied by a Float64 literal such as 0.5.\n\nUnlike the other checks, this one has no runtime component.  Julia's promotion mechanism implements mixed-type arithmetic by converting the operands to a common type, and the Precision check works by making that implicit conversion a MethodError — a plain dispatch failure, visible to static analyzers, that carries an explanatory hint:\n\njulia> Checked{Float32}(1) + 0.5\nERROR: MethodError: no method matching mixed_precision(::MathChecker.PrecisionMismatch{Checked{Float32, true, true, true, false, false, false, false}, Float64}, ::Float64)\nThe function `mixed_precision` exists, but no method is defined for this combination of argument types.\n\nMathChecker: the type Checked{Float32, true, true, true, false, false, false, false} has the `Precision` check enabled, which forbids implicitly mixing it with Float64.\n  This usually means a `Float32` computation was contaminated by a `Float64` (a literal like `0.5`, or a value from another part of the code).\n  If the mixing is intentional, convert explicitly (e.g. `Float32(x)` or `Checked{Float32}(x)`), permit the type by setting the `Precision` flag to `Float64` instead of `true` (`checked(x; precision=Float64)`), or turn the check off.\n\nMixing is always permitted with the exact number types, which carry no precision of their own: Integer (including Bool and BigInt), Rational, and AbstractIrrational (π, ℯ, …).  So 2x, x / 3, x + 1//2, and π * x are all fine and produce a Checked{T}.\n\nSetting the flag to a Type instead of true whitelists additional types.  Values of a whitelisted type are converted to T — the checked value keeps its own precision:\n\njulia> d = checked(Double64(1); precision=Float64);   # allow Float64 constants\n\njulia> typeof(d * 0.5)\nChecked{Double64, Float64, true, true, false, false, false, false}\n\nA Union whitelists several types.  A strict Checked (flag true) always refuses a value of another float type, even one that whitelists it, so strictness is never lost in a mixed expression.\n\nWhat is and is not caught.  The check applies to every path that goes through convert or promote: arithmetic and comparison operators, hypot, atan, fma, min/max, array construction ([x, 0.5]), and assignment into a typed array (v::Vector{Float64}; v[i] = x, when x is a strict Checked{Float32}).  It does not apply to:\n\nExplicit constructors: Float64(x), Checked{Float64}(x), and checked(x) are deliberate conversions and always work.\nConversion to a third-party float type (Double64(x), or storing into a Vector{Double64}), which goes through that type's own (::Type{Foo})(x::Real) constructor.  The reverse direction — a Double64 entering a Checked{Float64} computation — is caught, because it goes through convert(::Type{<:Checked}, x).\nThird-party mixed-type methods.  If a package defines its own <(::Foo, ::Real), that method is more specific than Julia's promotion fallback and bypasses the check (DoubleFloats does this for comparisons).  Arithmetic is unaffected.","title":"Precision"},{"category":"section","location":"10-checks/#NaN","page":"The checks","text":"Signals a NaNError when an operation produces a NaN, or when any operand of an operation is a NaN.  This gives a NaN the semantics of a signaling NaN: the first operation that touches it throws, with a stack trace to the culprit.  The message distinguishes the three stages of a NaN's life (the terminology is TrackedFloats.jl's):\n\ngenerated — 0.0 / 0.0, Inf - Inf: a NaN appears from non-NaN operands (IEEE's INVALID exception);\npropagated — NaN + 1.0: a NaN operand gives a NaN result;\nconsumed — NaN < 1.0 → false, NaN^0 → 1.0, copysign(1.0, NaN) → 1.0: a NaN operand gives an ordinary result.  This is the dangerous case, where a detectable problem silently becomes a wrong answer; a maximum written with < will happily skip a NaN and return the wrong element.\n\nConstructing a Checked from NaN does not signal, so sentinel values can be created. The classic use is detecting reads of uninitialized memory:\n\nA = fill(Checked{Float64}(NaN), n)   # instead of Vector{Float64}(undef, n)\n# ... fill in *some* elements ...\nsum(A)                               # throws at the first element never assigned\n\nThe total-order functions ==, isequal, and isless and the inspection functions isnan, isfinite, etc. are not checked, so that sort, unique, Dicts, hash, and testing for NaN keep working on arrays containing sentinels.","title":"NaN"},{"category":"section","location":"10-checks/#Inf","page":"The checks","text":"Signals an InfError when an operation produces +Inf or -Inf. Constructing a Checked from Inf, typemax, floatmax, comparisons with infinity, and isinf do not signal.","title":"Inf"},{"category":"section","location":"10-checks/#Cancellation","page":"The checks","text":"Signals a CancellationError when an addition or subtraction — including the additive step of fma(a, b, c) and muladd(a, b, c) — produces a result much smaller than its operands:\n\na pm b  mathrmtol cdot max(a b)\n\nThe default tolerance (flag true) is sqrt(eps(T)), i.e. the check fires when at least half of the significant bits cancelled.  A Float64 flag sets a different relative tolerance: checked(x; cancellation=1e-3) fires when more than about ten bits are lost.  The error reports the number of bits lost, also available from bitslost.\n\nA result of exactly zero from nonzero operands (x - x) is the most complete cancellation possible and does trigger the check.  Results that are not finite are never flagged (they are left to the Inf and NaN checks).\n\nnote: Cancellation is a heuristic\nThe subtraction of two nearby floating-point numbers is itself exact (Sterbenz' lemma).  The danger of cancellation is that whatever relative error the operands already carried is magnified in the result, by the same factor the magnitude shrank.  This check therefore points at where accuracy may have been lost; whether it actually was depends on the provenance of the operands. Expect some false positives in code that deliberately computes small differences, and use a non-throwing handler to survey them.","title":"Cancellation"},{"category":"section","location":"10-checks/#Absorption","page":"The checks","text":"Signals an AbsorptionError when, in an addition or subtraction (including the additive step of fma/muladd), one operand is so small relative to the other that it is partly or entirely lost.  This is the absorption of the floating-point arithmetic literature (e.g.  Muller et al., Handbook of Floating-Point Arithmetic); the numerical-linear-algebra literature calls the same thing swamping.\n\nWith the flag true, the test is exact: it fires when the result equals the larger operand although the smaller one is nonzero — the small operand made no difference.  1.0 + 1e-20 is the canonical example.  With a Float64 tolerance, the check instead fires when\n\ntextsmall  mathrmtol cdot textlarge\n\nwhich catches partial absorption, where the low-order bits of the small operand are lost.  The error reports which operand was absorbed. Adding zero is never an absorption, and results that are not finite are never flagged.\n\nnote: Double-double types rarely absorb\nA Double64 stores a value as an unevaluated sum of two Float64s, so 1 + 1e-300 is exactly representable and the exact test never fires.  Use a tolerance with such types.","title":"Absorption"},{"category":"section","location":"10-checks/#Subnormal","page":"The checks","text":"Signals a SubnormalError when an operation produces a subnormal (denormal) number.  Subnormals have fewer significant bits than normal numbers and are often dramatically slower to compute with. Zero is not subnormal.  Constructing a Checked from a subnormal does not signal.\n\nThe subnormal test uses MathChecker.issubnormal, which falls back to false for types that do not implement Base.issubnormal (BigFloat and Double64 have no subnormals in the IEEE sense).  The underflow-to-zero test still applies to such types: MPFR, for instance, flushes any BigFloat below floatmin(BigFloat) to exactly zero.","title":"Subnormal"},{"category":"section","location":"10-checks/#Rounding","page":"The checks","text":"Signals a RoundingError when +, -, `,/, orsqrtproduces a result that differs from the exact mathematical result — that is, whenever the operation rounded.*  The residual is computed with an error-free transformation (TwoSum for+and-;fma-based residuals for*,/, andsqrt) and reported in the error.Tmust supportfma`.\n\nThis is an extremely strict check: 0.1 + 0.2 fails it.  It is intended for verifying the steps of an algorithm that are designed to be exact — splitting a number into high and low parts, compensated summation kernels, double-double building blocks — rather than for general use.  Other operations (exp, sin, hypot, …) are not checked for rounding.","title":"Rounding"},{"category":"section","location":"10-checks/#Relation-to-the-IEEE-754-exception-flags","page":"The checks","text":"The IEEE 754 standard defines five exceptions that hardware signals through status flags.  Each has a counterpart here, evaluated per operation on the values themselves rather than by reading the flags (which Julia does not expose portably):\n\nIEEE exception MathChecker flag Notes\nINVALID NaN Stricter: also signals when a quiet NaN operand is propagated or consumed by any operation.  Julia already throws DomainError for sqrt(-1.0) and log(-1.0).\nDIVBYZERO Inf Reported as \"division by zero\" in the message.\nOVERFLOW Inf Reported as \"overflow\" in the message.  Only the round-to-nearest case (a result of ±Inf) is detectable; under directed rounding an overflow saturates to floatmax.\nUNDERFLOW Subnormal Subnormal results, plus underflow to zero where the exact result is known to be nonzero.  An exactly representable subnormal also counts.\nINEXACT Rounding Exact for +, -, *, /, sqrt via error-free transformations.  Not available for other functions (exp, sin, …), whose results are almost always inexact and would need higher-precision reference values.","title":"Relation to the IEEE 754 exception flags"},{"category":"section","location":"95-reference/#reference","page":"Reference","text":"","title":"Reference"},{"category":"section","location":"95-reference/#Contents","page":"Reference","text":"Pages = [\"95-reference.md\"]\nDepth = 2:3","title":"Contents"},{"category":"section","location":"95-reference/#The-type","page":"Reference","text":"","title":"The type"},{"category":"section","location":"95-reference/#Errors","page":"Reference","text":"","title":"Errors"},{"category":"section","location":"95-reference/#Handlers","page":"Reference","text":"","title":"Handlers"},{"category":"section","location":"95-reference/#Internals","page":"Reference","text":"","title":"Internals"},{"category":"section","location":"95-reference/#Index","page":"Reference","text":"Pages = [\"95-reference.md\"]","title":"Index"},{"category":"module","location":"95-reference/#MathChecker.MathChecker","page":"Reference","text":"MathChecker\n\nFloating-point numbers that check their own arithmetic.\n\nWrap a value in Checked and use it exactly as you would the underlying float. After every operation, the checks selected by the type's flag parameters inspect the result and signal an error pointing at the offending operation:\n\nChecked{T, Precision, NaN, Inf, Cancellation, Absorption, Subnormal, Rounding}\n\nFlag Signals when …\nPrecision mixed with a float of a different type (a dispatch error)\nNaN a NaN is produced or used as an operand\nInf ±Inf is produced\nCancellation a ± b cancels most significant bits\nAbsorption an addend is too small to register\nSubnormal a subnormal is produced\nRounding + - * / sqrt is inexact\n\nusing MathChecker\nx = checked(1e-8; cancellation=true)\n(1 + x) - 1        # throws CancellationError\n\nDisabled checks are eliminated by the compiler, so a Checked{Float64} with every flag false is exactly as fast as a Float64.  See handler and collect_failures for reporting failures without throwing.\n\n\n\n\n\n","title":"MathChecker.MathChecker"},{"category":"type","location":"95-reference/#MathChecker.Checked","page":"Reference","text":"Checked{T<:AbstractFloat, Precision, NaN, Inf, Cancellation, Absorption, Subnormal, Rounding} <: AbstractFloat\n\nA floating-point number of type T whose every operation is verified by the checks selected by the flag type parameters.  A Checked participates in arithmetic exactly like a T — all of Base's arithmetic, math, comparison, rounding, and conversion functions are supported — but after every operation the enabled checks inspect the result (and operands) and signal a CheckError through the current handler if something is wrong.  Disabled checks cost nothing: they are eliminated at compile time.\n\nFlags\n\nEach flag is false (off) or true (on).  Three of them accept a configuration value in place of true:\n\nFlag Signals when … Instead of true …\nPrecision implicitly mixed with a float of another type a Type of extra permitted types\nNaN a NaN is produced, or used as an operand \nInf ±Inf is produced \nCancellation a ± b loses more than half its significant bits a Float64 relative tolerance\nAbsorption an addend is too small to change the result at all a Float64 relative tolerance\nSubnormal a subnormal number is produced \nRounding +, -, *, /, or sqrt is not exact \n\nEach flag is documented in detail in the manual's \"Checks\" section.  The defaults, used by Checked{T}(x) and checked, turn on Precision, NaN, and Inf — the three checks that can never produce a false positive — and leave the rest off.\n\nConstructors\n\nChecked{T, flags...}(x)                 # x::Real; converts x to T; never signals\nChecked{T}(x; nan=true, inf=true, …)    # defaults for flags not given\nChecked(x; …)                           # T = typeof(float(x))\nchecked(x; …)                           # also handles arrays, tuples, complexes\n\nConstructors never signal, even for NaN or Inf, so that sentinels can be created (see the NaN check).  Constructors are also exempt from the Precision check: they are explicit conversions.  The functions checked and unchecked wrap and unwrap values, arrays, tuples, and complex numbers; flags and valuetype recover the parameters.\n\nExamples\n\njulia> x = Checked(1.0)\nChecked{Float64}(1.0)\n\njulia> x / 0\nERROR: InfError: 1.0 / 0.0 produced Inf:\n       division by zero (the exact result is infinite).\n[...]\n\njulia> unchecked(sqrt(x + 1))\n1.4142135623730951\n\njulia> y = checked(1e-9; cancellation=true)\nChecked{Float64, true, true, true, true, false, false, false}(1.0e-9)\n\njulia> (1 + y) - 1\nERROR: CancellationError: 1.000000001 - 1.0 produced 1.000000082740371e-9,\n       cancelling 29.9 of 53 significant bits: |result| / max(|a|, |b|) = 1e-09\n       is below the tolerance 1.49e-08 (26 bits).\n[...]\n\nPromotion\n\nTwo Checked values with the same T promote to a Checked with the union of their flags, so a value checked for NaN added to a value checked for Inf is checked for both.  A Checked{T} combined with a plain T, Integer, Rational, or irrational constant yields a Checked{T}.  Combining with a different floating-point type follows Julia's usual promotion unless the Precision flag is set, in which case the checked value keeps its own T and the conversion of the other operand is a MethodError (with an explanatory hint) unless that type is permitted.\n\n\n\n\n\n","title":"MathChecker.Checked"},{"category":"function","location":"95-reference/#MathChecker.checked","page":"Reference","text":"checked(x; precision=true, nan=true, inf=true, cancellation=false, absorption=false,\n           subnormal=false, rounding=false)\nchecked(T::Type{<:AbstractFloat}; flags...)\n\nWrap x in a Checked with the given check flags (see Checked for their meanings and accepted values); flags not mentioned take their defaults.\n\nx may be a real number, a Complex, a Tuple, or an AbstractArray of any of these; containers are wrapped elementwise.  Integers and rationals are converted to Float64 first.  Applied to a Checked value, the flags given are changed and the others kept, so checked(x; cancellation=true) adds a check to x.\n\nApplied to a floating-point type, checked returns the corresponding Checked type, which is convenient with zeros, rand, etc.:\n\nzeros(checked(Float64; nan=true), 3)\n\nSee also unchecked.\n\n\n\n\n\n","title":"MathChecker.checked"},{"category":"function","location":"95-reference/#MathChecker.unchecked","page":"Reference","text":"unchecked(x)\n\nRemove the Checked wrapper from x, returning the underlying value. Complexes, Tuples, and AbstractArrays are unwrapped elementwise; anything else is returned unchanged, so unchecked can be applied blindly to the result of a computation.\n\nSee also checked.\n\n\n\n\n\n","title":"MathChecker.unchecked"},{"category":"function","location":"95-reference/#MathChecker.flags","page":"Reference","text":"flags(x::Checked)\nflags(::Type{<:Checked})\n\nThe check flags of x as a NamedTuple with fields precision, nan, inf, cancellation, absorption, subnormal, and rounding, in that order (the order of the type parameters).\n\n\n\n\n\n","title":"MathChecker.flags"},{"category":"function","location":"95-reference/#MathChecker.valuetype","page":"Reference","text":"valuetype(x::Checked)\nvaluetype(::Type{<:Checked})\n\nThe underlying floating-point type T of a Checked{T, …}.  For any other type, the type itself.\n\n\n\n\n\n","title":"MathChecker.valuetype"},{"category":"constant","location":"95-reference/#MathChecker.DEFAULT_FLAGS","page":"Reference","text":"DEFAULT_FLAGS\n\nThe flags used when none are specified: Precision, NaN, and Inf on; everything else off.\n\n(precision = true, nan = true, inf = true, cancellation = false, absorption = false,\n subnormal = false, rounding = false)\n\n\n\n\n\n","title":"MathChecker.DEFAULT_FLAGS"},{"category":"type","location":"95-reference/#MathChecker.CheckError","page":"Reference","text":"CheckError <: Exception\n\nSupertype of the exceptions signalled by the runtime checks.  Every subtype has at least the fields\n\nop: the function that was being evaluated (e.g. +, sqrt),\nresult: the (unwrapped) value the operation produced, and\nargs::Tuple: the (unwrapped) operands.\n\nThe concrete subtypes are NaNError, InfError, SubnormalError, RoundingError, CancellationError, and AbsorptionError.\n\n\n\n\n\n","title":"MathChecker.CheckError"},{"category":"type","location":"95-reference/#MathChecker.NaNError","page":"Reference","text":"NaNError(op, result, args)\n\nSignalled by the NaN check when op(args...) produced a NaN, or had a NaN operand. The message says whether the operation generated the NaN (no operand was NaN), propagated it (NaN in, NaN out), or consumed it (NaN in, ordinary value out — the point at which a NaN silently becomes a wrong answer).\n\n\n\n\n\n","title":"MathChecker.NaNError"},{"category":"type","location":"95-reference/#MathChecker.InfError","page":"Reference","text":"InfError(op, result, args)\n\nSignalled by the Inf check when op(args...) produced ±Inf.  The message says whether this was a division by zero (an exact infinity), an overflow, or the propagation of an already-infinite operand.\n\n\n\n\n\n","title":"MathChecker.InfError"},{"category":"type","location":"95-reference/#MathChecker.SubnormalError","page":"Reference","text":"SubnormalError(op, result, args)\n\nSignalled by the Subnormal check when op(args...) produced a subnormal number, or underflowed to zero although its exact result is nonzero.\n\n\n\n\n\n","title":"MathChecker.SubnormalError"},{"category":"type","location":"95-reference/#MathChecker.RoundingError","page":"Reference","text":"RoundingError(op, result, args, residual)\n\nSignalled by the Rounding check when op(args...) was not exact.  residual is the (approximate) rounding error exact - result, as computed by an error-free transformation.\n\n\n\n\n\n","title":"MathChecker.RoundingError"},{"category":"type","location":"95-reference/#MathChecker.CancellationError","page":"Reference","text":"CancellationError(op, result, args, ratio, tolerance)\n\nSignalled by the Cancellation check when an addition or subtraction lost most of its significant bits.  ratio is the measured |result| / max(|a|, |b|) and tolerance the relative threshold it fell below.  The number of bits lost is available from bitslost.\n\n\n\n\n\n","title":"MathChecker.CancellationError"},{"category":"type","location":"95-reference/#MathChecker.AbsorptionError","page":"Reference","text":"AbsorptionError(op, result, args, absorbed, ratio, tolerance)\n\nSignalled by the Absorption check when an operand of an addition or subtraction was too small to (fully) register in the result (\"absorption\", also called \"swamping\"). absorbed is the index into args of the operand that was lost, ratio is the measured |small| / |large| of the two addends, and tolerance is the relative threshold used, or nothing for the exact test.\n\n\n\n\n\n","title":"MathChecker.AbsorptionError"},{"category":"function","location":"95-reference/#MathChecker.bitslost","page":"Reference","text":"bitslost(err::CancellationError)\n\nThe number of significant bits cancelled: log2(max(|a|, |b|) / |a ± b|), or Inf if the result was exactly zero.\n\n\n\n\n\n","title":"MathChecker.bitslost"},{"category":"constant","location":"95-reference/#MathChecker.handler","page":"Reference","text":"MathChecker.handler\n\nA ScopedValue holding the function that is called with a CheckError whenever a check fails.  The default handler is throw.  Other handlers are free to log, record, or ignore the error; if the handler returns, the operation that failed the check returns its (possibly NaN, infinite, …) result as usual.\n\nUse with_handler to change the handler for the dynamic extent of a function call, or ScopedValues.with(MathChecker.handler => f) do … end directly.  Because it is a scoped value, the setting is task-local and safe to use from multiple threads.\n\nProvided handlers: throw (default), warn_handler, and the collecting handler used by collect_failures.\n\n\n\n\n\n","title":"MathChecker.handler"},{"category":"function","location":"95-reference/#MathChecker.with_handler","page":"Reference","text":"with_handler(f, h)\n\nCall f() with MathChecker.handler set to h for the dynamic extent of the call.  h is a function of one argument, a CheckError.\n\nwith_handler(warn_handler) do\n    risky_computation(checked(x))\nend\n\n\n\n\n\n","title":"MathChecker.with_handler"},{"category":"function","location":"95-reference/#MathChecker.warn_handler","page":"Reference","text":"warn_handler(err::CheckError)\n\nA handler that logs each failed check as a warning (including a backtrace to the offending operation) and lets the computation continue.\n\n\n\n\n\n","title":"MathChecker.warn_handler"},{"category":"function","location":"95-reference/#MathChecker.collect_failures","page":"Reference","text":"collect_failures(f) -> (result, failures::Vector{CheckError})\n\nRun f() with a handler that records every failed check instead of throwing, and return the result of f together with the vector of recorded CheckErrors.  This is the recommended way to survey all the numerical problems in a computation at once, or to assert that there are none:\n\nresult, failures = collect_failures() do\n    myalgorithm(checked(A; cancellation=true))\nend\n@test isempty(failures)\n\n\n\n\n\n","title":"MathChecker.collect_failures"},{"category":"function","location":"95-reference/#MathChecker.fail","page":"Reference","text":"MathChecker.fail(err::CheckError)\n\nReport a failed check by calling the current handler.  The checks call this rather than throw, so that users can switch handlers.\n\n\n\n\n\n","title":"MathChecker.fail"},{"category":"function","location":"95-reference/#MathChecker.runchecks","page":"Reference","text":"MathChecker.runchecks(::Type{X<:Checked}, op, result, args...)\n\nRun the checks enabled in the type X on result = op(args...) (all unwrapped).  The NaN, Inf, and Subnormal checks run first, so that a non-finite result is reported as such rather than as a cancellation or absorption.\n\n\n\n\n\n","title":"MathChecker.runchecks"},{"category":"function","location":"95-reference/#MathChecker.issubnormal","page":"Reference","text":"MathChecker.issubnormal(x)\n\nLike Base.issubnormal, but defined (as false) for any Real, so that the Subnormal check can be used with types that do not implement Base.issubnormal.  Extend this for such types if they can hold subnormal values.\n\n\n\n\n\n","title":"MathChecker.issubnormal"},{"category":"function","location":"95-reference/#MathChecker.hasprecision","page":"Reference","text":"MathChecker.hasprecision(::Type{<:Checked}) -> Bool\n\nWhether the type's Precision flag is set (to true or to a whitelist type).\n\n\n\n\n\n","title":"MathChecker.hasprecision"},{"category":"type","location":"95-reference/#MathChecker.PrecisionMismatch","page":"Reference","text":"MathChecker.PrecisionMismatch{X, Y}\n\nTrait type marking that values of types X and Y may not be implicitly converted into one another, because at least one of them is a Checked with the Precision flag that does not permit the other.  There is deliberately no method of MathChecker.mixed_precision for this type, so that the attempt fails with a MethodError (which carries an explanatory hint).\n\n\n\n\n\n","title":"MathChecker.PrecisionMismatch"},{"category":"function","location":"95-reference/#MathChecker.mixed_precision","page":"Reference","text":"MathChecker.mixed_precision(trait, x)\n\nIdentity on x when trait is PrecisionOK().  Deliberately has no method for a PrecisionMismatch, so that implicitly mixing a Checked that has the Precision flag with a float of another type is a MethodError.\n\n\n\n\n\n","title":"MathChecker.mixed_precision"},{"category":"function","location":"95-reference/#MathChecker.formatcall","page":"Reference","text":"MathChecker.formatcall(op, args) -> String\n\nThe call op(args...) in a readable form: infix for binary operators (\"a + b\"), functional otherwise (\"sqrt(a)\").\n\n\n\n\n\n","title":"MathChecker.formatcall"},{"category":"function","location":"95-reference/#MathChecker.message","page":"Reference","text":"MathChecker.message(err::CheckError) -> String\n\nThe full text of the error message, as printed by showerror.  Continuation lines are indented to line up under the error name after the REPL's ERROR: prefix.\n\n\n\n\n\n","title":"MathChecker.message"},{"category":"section","location":"30-design/#Design-notes","page":"Design notes","text":"","title":"Design notes"},{"category":"section","location":"30-design/#How-a-checked-operation-works","page":"Design notes","text":"Every supported Base function has a method for Checked of the form\n\nBase.sqrt(a::X) where {X<:Checked} = _wrap(X, sqrt, sqrt(a.val), a.val)\n\nThe operation is performed on the unwrapped value by the wrapped type's own method, then _wrap runs the checks and re-wraps the result:\n\nfunction _wrap(::Type{X}, op, result, args...) where {X<:Checked}\n    runchecks(X, op, result, args...)\n    return X(result)\nend\n\nrunchecks is a sequence of flag && check_…(op, result, args...) statements whose flags are the type parameters of X.  Since they are compile-time constants, the compiler deletes the disabled checks entirely; the enabled ones inline to a comparison or two.  Each check_… function dispatches on typeof(op), so a check that does not apply to the operation at hand (cancellation on sqrt, say) is a no-op method that also compiles away.  A failing check calls MathChecker.fail, which is @noinline and invokes the handler, so the failure path stays out of the hot loop.\n\nConsequently, a Checked{Float64} with all flags off generates the same LLVM instructions as a Float64, and one with the NaN and Inf checks on adds two well-predicted branches per operation.  The value is an immutable struct with a single field, so arrays of Checked have the same memory layout as arrays of T.","title":"How a checked operation works"},{"category":"section","location":"30-design/#Operations-are-primitives","page":"Design notes","text":"Every Base function with a Checked method — hypot, sinpi, mod, round, and so on — is treated as a primitive: the wrapped type's implementation runs unobserved, and only the final result is checked.  Base's generic hypot, for instance, internally computes essentially 1 + ϵ*ϵ with a tiny ϵ, which would trip the Absorption check; because hypot is a primitive, it does not.  The checks therefore report on your arithmetic, not on the internals of the library functions you call.\n\nFunctions that have no Checked method fall back to Julia's generic implementations, which are built from the primitives and are therefore checked step by step.  This is usually what one wants for user code, and occasionally noisy for library code (a generic isapprox, say, computes x - y); isapprox in particular is special-cased to work on the raw values.  A full list of the primitive methods is in src/operations.jl.","title":"Operations are primitives"},{"category":"section","location":"30-design/#Promotion","page":"Design notes","text":"Binary operations are defined only for two Checked values of the same type; every mixed call reaches them through Julia's promotion machinery, so the rules live in a few promote_rule methods:\n\nTwo Checked values with the same T and different flags promote to the union of their flags (true beats a configured value; two configured tolerances take the stricter; two whitelists take their Union).  A value checked for NaN added to one checked for Inf is checked for both.\nA Checked{T} with a plain number follows Julia's rules for T — Checked{Float32} + Int is a Checked{Float32}, `Checked{Float32}\nFloat64is aChecked{Float64}— *unless* thePrecision` flag\nis set, in which case the checked value keeps its own T and the other operand must be convertible to it without loss of the check's guarantee.\nIrrationals adopt the precision of the Checked value, as they do for Float32; Base's rules for BigFloat and BigInt are overridden symmetrically so that both directions of promote_type agree.","title":"Promotion"},{"category":"section","location":"30-design/#Constructors-are-explicit,-conversions-are-implicit","page":"Design notes","text":"Julia distinguishes constructing a value (Float64(x)) from converting one implicitly (convert(Float64, x), which is what promotion, setindex!, and typed fields use).  MathChecker leans on this distinction:\n\nConstructors — Checked{T}(x), Float64(x::Checked), checked — never signal and ignore the Precision check. They are how sentinels (NaN) are created and how the user says \"yes, I mean it\".\nConversions — convert(::Type{<:Checked}, x) and convert(::Type{<:BaseFloat}, x::Checked) — enforce the Precision check by dispatching, for an incompatible pair, to a function (MathChecker.mixed_precision) that has no matching method.\n\nThe resulting MethodError is a genuine dispatch failure — the same thing JET or a type-inference report would flag — rather than an exception thrown from inside a matched method.  An error hint registered with Base.Experimental.register_error_hint (guarded as its docstring recommends) explains the situation and the three ways out.","title":"Constructors are explicit, conversions are implicit"},{"category":"section","location":"30-design/#Limitations","page":"Design notes","text":"Third-party float types.  The generic (::Type{S<:AbstractFloat})(x::Checked) constructor and the strict convert are restricted to Base's float types, because packages such as DoubleFloats define (::Type{Double64})(x::Real) constructors that would otherwise be ambiguous.  Converting a Checked to such a type therefore goes through the type's own constructor and is not precision-checked; converting from it is. Likewise, mixed-type methods a package defines itself (DoubleFloats' <(::Double64, ::Real)) bypass promotion and hence the check.\nNaNMath and friends.  Packages that provide their own math functions with concrete-type methods (NaNMath.sin(::Float64)) do not know about Checked; a package extension would be needed.\nLAPACK.  Anything that requires BLAS/LAPACK (eigen, svd) does not work for any non-BLAS float type, Checked included; use GenericLinearAlgebra or GenericSchur.\nKeyword constructors and inference.  checked(x; nan=true) produces a concrete type only through constant propagation of the keyword values.  This works at ordinary call sites with literal flags, but in performance-critical code prefer the positional form Checked{T, flags...}(x) or construct the type once with checked(T; ...).","title":"Limitations"},{"category":"section","location":"20-handling-failures/#handling-failures","page":"Handling failures","text":"When a check fails, the operation constructs a CheckError describing what happened and passes it to the current handler, a function stored in the scoped value MathChecker.handler. The default handler is throw.","title":"Handling failures"},{"category":"section","location":"20-handling-failures/#The-errors","page":"Handling failures","text":"Every error records the operation op, its unwrapped operands args, and the unwrapped result, and prints as a readable description of the failing call:\n\njulia> checked(1.0) / 0\nERROR: InfError: 1.0 / 0.0 produced Inf:\n       division by zero (the exact result is infinite).\n\njulia> checked(1e-9; cancellation=true) + 1 - 1\nERROR: CancellationError: 1.000000001 - 1.0 produced 1.000000082740371e-9,\n       cancelling 29.9 of 53 significant bits: |result| / max(|a|, |b|) = 1e-09\n       is below the tolerance 1.49e-08 (26 bits).\n\njulia> checked(1.0; absorption=true) + 1e-20\nERROR: AbsorptionError: 1.0 + 1.0e-20 produced 1.0; the operand 1.0e-20 was absorbed:\n       |small| / |large| = 1e-20, so it made no difference to the result\n       (anything below ≈ 1.11e-16 would be lost).\n\njulia> checked(0.1; rounding=true) + 0.2\nERROR: RoundingError: 0.1 + 0.2 produced 0.30000000000000004, which is inexact:\n       exact − computed ≈ -2.7755575615628914e-17 (0.5 ulps).\n\nError Extra fields\nNaNError \nInfError \nSubnormalError \nRoundingError residual (exact − computed)\nCancellationError ratio (measured), tolerance; see bitslost\nAbsorptionError absorbed (index into args), ratio, tolerance\n\nEvery message shows the operands and result in full, and the threshold checks show the measured quantity next to the threshold it violated, in the same units.  Long messages continue on lines indented to line up under the error name; the whole message is available as a string from MathChecker.message.\n\nBecause the error is thrown from inside the arithmetic operator, the stack trace leads directly to the line of user code that performed the operation.","title":"The errors"},{"category":"section","location":"20-handling-failures/#Surveying-all-failures","page":"Handling failures","text":"Throwing at the first problem is right for hunting a NaN, but the heuristic checks are more useful when one can see every place they fire.  collect_failures runs a function with a recording handler and returns its result together with the list of failures:\n\nresult, failures = collect_failures() do\n    myalgorithm(checked(A; cancellation=true, absorption=true))\nend\nfor f in failures\n    println(sprint(showerror, f))\nend\n\nThis also makes a convenient test assertion:\n\n@test isempty(last(collect_failures(() -> myalgorithm(checked(A; cancellation=1e-3)))))\n\nwarn_handler instead logs each failure as a warning, with a backtrace, and lets the computation continue:\n\nwith_handler(warn_handler) do\n    myalgorithm(checked(A))\nend","title":"Surveying all failures"},{"category":"section","location":"20-handling-failures/#Logging-to-a-file,-with-stack-traces","page":"Handling failures","text":"A handler can record where each failure happened as well as what it was.  This one writes every message to a file together with the stack trace of the user code that triggered it (frames inside MathChecker itself are dropped):\n\nusing MathChecker: message\n\nfunction file_logger(io::IO)\n    return function (err)\n        println(io, message(err))\n        frames = filter(f -> !occursin(\"MathChecker\", String(f.file)), stacktrace())\n        Base.show_backtrace(io, frames)\n        println(io, '\\n')\n    end\nend\n\nopen(\"mathchecker.log\", \"w\") do io\n    with_handler(file_logger(io)) do\n        myalgorithm(checked(A; cancellation = true))\n    end\nend\n\nCapturing a stack trace is slow (microseconds to milliseconds), which does not matter for a few failures but would for millions; cap the count in the handler if that is a risk.","title":"Logging to a file, with stack traces"},{"category":"section","location":"20-handling-failures/#Custom-handlers","page":"Handling failures","text":"with_handler(f, h) calls f() with the handler set to any function h of one argument.  If h returns, the operation that failed the check returns its (possibly non-finite) result and the computation proceeds.  For example, to count failures by type:\n\ncounts = Dict{DataType,Int}()\nwith_handler(() -> myalgorithm(checked(A; absorption=true))) do err\n    counts[typeof(err)] = get(counts, typeof(err), 0) + 1\nend\n\nThe handler is a ScopedValue, so it is dynamically scoped — it applies to everything called from f, however deep — and task-local, so it is safe to use different handlers on different threads.  It can also be set directly with ScopedValues.with:\n\nusing ScopedValues\nwith(MathChecker.handler => err -> nothing) do\n    ...   # ignore all failures\nend\n\nCustom handlers are only invoked on the failure path, so they add no cost to operations whose checks pass.","title":"Custom handlers"},{"category":"section","location":"#MathChecker","page":"MathChecker","text":"Floating-point numbers that check their own arithmetic.\n\nwarning: Written with Claude\nThe design of this package was specified by me, Mike Boyle, but the implementation, tests, and this documentation were produced by Anthropic's Claude, and then I reviewed it.  Please read the code with that in mind, and report anything suspicious as an issue.\n\nMathChecker provides one type, Checked, which wraps any floating-point number and behaves exactly like it in arithmetic — except that after every operation, a set of checks selected by the type's parameters inspects the result and signals an error that points at the offending operation.  The checks are:\n\nFlag Signals when …\nPrecision the value is implicitly mixed with a float of a different type\nNaN a NaN is produced, or used as an operand of any operation\nInf ±Inf is produced\nCancellation an addition or subtraction cancels most of its significant bits\nAbsorption an addend is too small to change the result\nSubnormal a subnormal number is produced\nRounding +, -, *, /, or sqrt is not exact\n\nBecause the flags are type parameters, checks that are turned off are eliminated by the compiler: a Checked{Float64} with every flag off compiles to the same instructions as a Float64.","title":"MathChecker"},{"category":"section","location":"#Installation","page":"MathChecker","text":"The package is not yet registered.  Install it from GitHub:\n\nusing Pkg\nPkg.add(url=\"https://github.com/moble/MathChecker.jl\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"MathChecker","text":"Wrap the inputs of a computation and run it as usual:\n\njulia> using MathChecker\n\njulia> x = Checked(1.0)          # the wrapped type is taken from the argument\nChecked{Float64}(1.0)\n\njulia> y = sqrt(x + 1) * 2       # ordinary arithmetic, checked at every step\nChecked{Float64}(2.8284271247461903)\n\njulia> unchecked(y)              # get the plain value back\n2.8284271247461903\n\nBy default the Precision, NaN, and Inf checks are on — the three that never produce false positives.  When something goes wrong, the error names the operation and its operands:\n\njulia> x / 0\nERROR: InfError: 1.0 / 0.0 produced Inf:\n       division by zero (the exact result is infinite).\n[...]\n\njulia> (x - 1) / (x - 1)\nERROR: NaNError: 0.0 / 0.0 produced NaN (no operand was NaN).\n[...]\n\njulia> Checked{Float32}(1) + 0.5     # a Float64 literal contaminating a Float32 computation\nERROR: MethodError: no method matching mixed_precision(::MathChecker.PrecisionMismatch{Checked{Float32, true, true, true, false, false, false, false}, Float64}, ::Float64)\n[...]\n\nThe other checks are enabled with keyword arguments to checked (or, equivalently, as positional type parameters):\n\njulia> z = checked(1e-9; cancellation=true)\nChecked{Float64, true, true, true, true, false, false, false}(1.0e-9)\n\njulia> (1 + z) - 1\nERROR: CancellationError: 1.000000001 - 1.0 produced 1.000000082740371e-9,\n       cancelling 29.9 of 53 significant bits: |result| / max(|a|, |b|) = 1e-09\n       is below the tolerance 1.49e-08 (26 bits).\n[...]\n\nchecked and unchecked also work elementwise on arrays, tuples, and complex numbers, so an existing function can be checked without modification:\n\nresult = unchecked(myfunction(checked(A; absorption=true), checked(b; absorption=true)))\n\nTo see all of the problems in a computation rather than stopping at the first, use collect_failures (or another handler):\n\njulia> result, failures = collect_failures() do\n           w = checked(1.0)\n           (w / 0) * 0\n       end;\n\njulia> failures\n2-element Vector{CheckError}:\n InfError(/, Inf, (1.0, 0.0))\n NaNError(*, NaN, (Inf, 0.0))","title":"Quick start"},{"category":"section","location":"#Uses","page":"MathChecker","text":"Hunting a NaN or Inf. Wrap the inputs, and the first operation that produces a non-finite value throws, with a stack trace to the line that did it.  (This use, and the original implementation, come from a Discourse post by Brian Guenter.)\nDetecting the use of uninitialized memory. Fill an array with Checked{Float64}(NaN); constructors never signal, but the first arithmetic on an element that was never assigned will.\nKeeping a Float32, Double64, or BigFloat computation pure. With the Precision check, a stray Float64 literal — the classic way to silently lose the extra precision of a Double64 — becomes an error at the offending operation, with a hint explaining how to fix it.\nLocating loss of precision. The Cancellation and Absorption checks point at the additions and subtractions where relative accuracy is destroyed, and the Rounding check verifies that steps which are meant to be exact really are.","title":"Uses"},{"category":"section","location":"#Contents","page":"MathChecker","text":"Pages = [\"10-checks.md\", \"20-handling-failures.md\", \"30-design.md\", \"95-reference.md\"]\nDepth = 2","title":"Contents"}]
}
